[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cortex"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "requests",
    "beautifulsoup4",
]

[project.scripts]
cortex = "chatbot.core:run_chatbot"